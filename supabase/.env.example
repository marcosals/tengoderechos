# Tengo Derechos - Supabase Edge Functions Environment Variables Template
# Use these keys locally in supabase/functions/.env (or set them via supabase secrets set)

# OpenAI API Key used by Edge Functions to fetch embeddings and answer queries
OPENAI_API_KEY=your_openai_api_key
